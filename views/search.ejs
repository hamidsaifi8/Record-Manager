<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background: #f0f2f5;
            margin: 0;
            padding: 0;
        }

        .container {
            max-width: 400px;
            margin: 50px auto;
            background: white;
            padding: 30px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            border-radius: 10px;
        }

        h1 {
            text-align: center;
            color: #333;
        }

        form {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        input[type="text"] {
            flex: 1;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 5px;
        }

        button {
            padding: 10px 20px;
            background-color: #007bff;
            color: white;
            border: none;
            border-radius: 5px;
            cursor: pointer;
        }

        button:hover {
            background-color: #0056b3;
        }

        .output {
            background: #e9f7ef;
            padding: 20px;
            border-left: 5px solid #28a745;
            border-radius: 5px;
        }

        .output p {
            margin: 10px 0;
        }

        strong {
            color: #444;
        }

        p {
            color: #333;
        }
    </style>

</head>

<body>
    <div class="container">
        <h2>Search Student Data</h2>
        <form method="GET" action="/search">
            <input type="text" name="name" placeholder="Enter student name" value="<%= searchedName || '' %>"
                required />
            <button type="submit">Search</button>
        </form>

        <% if (student) { %>
            <div class="result">
                <h3>Student Found:</h3>
                <p><strong>Name:</strong>
                    <%= student.name %>
                </p>
                <p><strong>Age:</strong>
                    <%= student.age %>
                </p>
                <p><strong>Course:</strong>
                    <%= student.course %>
                </p>
                <p><strong>Email:</strong>
                    <%= student.email %>
                </p>
            </div>
            <% } else if (searchedName) { %>
                <p class="error">No match for "<%= searchedName %>"</p>
                <% } %>

                    <a href="/add">Go to Add Page</a>
    </div>
</body>

</html>